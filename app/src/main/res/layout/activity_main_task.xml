<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/colorWhite95"
    android:fitsSystemWindows="true">

    <include layout="@layout/toolbar_main_task" />

    <include layout="@layout/content_main_task_scrolling" />

    <include layout="@layout/bottom_sheet_add_task"/>

<!--    <com.google.android.material.floatingactionbutton.FloatingActionButton
        style="@style/fab_home_icon"
        android:id="@+id/fab_add"
        app:layout_anchor="@+id/bs_"
        android:layout_margin="@dimen/activity_margin_normal"
        app:layout_anchorGravity="top|end"
        android:backgroundTint="@color/colorFab_add"
        android:src="@drawable/ic_add" />-->

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add"
        style="@style/fab_main"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/activity_margin_normal"
        android:backgroundTint="@color/colorAccent"
        app:layout_anchor="@+id/bs_"
        android:src="@drawable/ic_add"
        app:layout_anchorGravity="top|end" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>